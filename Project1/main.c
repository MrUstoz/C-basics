#include <stdio.h>
#include <stdlib.h>

/*struct MyStruct
{
    int MyNum;
    char MyName[50];
};*/

/*enum Viloyatlar{
    Fergana,
    Tashkent,
    Andijan,
    Namangan
};*/

int main()
{
    /*enum Viloyatlar v1;

    v1 = Fergana;

    char* valiyatlar[] = {"Fergana", "Toshkent", "Andijan", "Namangan"};

    for(int i = 0; i < 4; i++)
    {
        printf("%s\n", valiyatlar[i]);
    }*/

    /*int maxsulotSoni = 50;
    float maxsulotNarxi = 9.99;
    float umumiyNarxi = maxsulotSoni * maxsulotNarxi;
    char belgi = '$';

    printf("%d\n", maxsulotSoni);
    printf("%f%c\n", maxsulotNarxi, belgi);
    printf("%f%c\n", umumiyNarxi, belgi);*/

    /*const float PI = 3.14;
    int a;
    printf("Son kiriting: ");
    scanf("%d", &a);

    printf("Output: %f", PI * a);*/

    /*int time = 11;
    char firstname[50];
    int* ptr = &time;
    fgets(firstname, sizeof(firstname), stdin);
    (time < 18) ? printf("Hayrli kun %p", ptr) : printf("Kayrli tun ");
    printf(firstname);*/

    /*FILE *fprt;

    fprt = fopen("D:\filename.txt", "r");

    fprintf(fprt, "Some text");

    char myString[100];

    fgets(myString, 100, fprt);

    fclose(fprt);*/

    /*struct MyStruct m1;

    m1.MyNum = 10;
    sprintf(m1.MyName, "Abdulvosid Foziljonov");

    printf("%d %s", m1.MyNum, m1.MyName);
*/

    /*double d, b;
    for(int i = 0; i < 3; i++)
    {
        scanf("%lf %lf", &d, &b);
        printf("Output: %lf \n", d + b);
    }*/

    return 0;
}





