#include <iostream>

using namespace std;

int main()
{

    //Masala - a sonni 100 dan katta yoki kichikligi tekshiring
    // +=, -=, *=, /=, >=, <=, ==, !=, ||, &&
    int a;
    cin >> a;
    if(a >= 5 && a <= 10) //
    {
        cout <<"a son 5 dan katta va 10 dan kichik";
    }
    else
    {
        cout <<"a son 5 dan kichik yoki 10 dan katta";
    }
    return 0;
}
